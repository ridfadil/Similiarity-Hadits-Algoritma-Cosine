<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="padilah.farid.hadits.similiarityhaditsfarid">

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="AA_DB_NAME"
            android:value="SimilarityHadits.db" />
        <meta-data
            android:name="AA_DB_VERSION"
            android:value="5" />
        <meta-data
            android:name="AA_DB_MODEL"
            android:value="padilah.farid.hadits.similiarityhaditsfarid.model.Hadits" />
        <meta-data
            android:name="AA_MODELS"
            android:value="padilah.farid.hadits.similiarityhaditsfarid.model.Hadits" />

        <activity
            android:name=".DashboardActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.TambahHadirsActivity" />
        <activity android:name=".activity.SimilarityHaditsActivity" />
        <activity android:name=".activity.LihatHaditsActivity" />
        <activity android:name=".activity.TentangActivity" />
        <activity android:name=".activity.UpdateHaditsActivity"></activity>
    </application>

</manifest>